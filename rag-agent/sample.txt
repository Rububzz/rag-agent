Retrieval Augmented Generation is a technique that combines information retrieval 
with text generation. Instead of relying solely on what a language model learned 
during training, RAG first searches a knowledge base for relevant information, 
then passes that information to the model as context when generating a response.
This allows the model to answer questions about documents it has never seen before.
